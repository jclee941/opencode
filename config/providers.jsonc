// Provider and model definitions.
// Merged into opencode.jsonc by: go run scripts/gen-opencode-config.go
//
// Provider inventory:
// - anthropic: Claude models via local proxy (Meridian at :3456)
// - minimax-coding-plan: Built-in MiniMax models (fast/cheap)
// - kimi-for-coding: Built-in Moonshot Kimi models  
// - opencode-go: Built-in GLM models
//
// NOTE: anthropic provider is explicitly defined here.
// Other providers (minimax-coding-plan, kimi-for-coding, opencode-go) use built-in definitions.

{
  "provider": {}
}
